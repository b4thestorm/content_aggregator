import unittest
from content.service import Feed

class FeedTest(unittest.TestCase):
    #test that it gets a rss feed (without going to the internet)
    #test that it gets a link string if it's available 
    #test that it doesn't get a link string if not available
    #test that it creates a new podcast link
    